package structural.adaptor;

public class AdaptorPattern {
    //Our application supports Stripe, Razorpay and Elavon. Each provider has a different API, but we want our application to call all of them through PaymentGateway."
    //That's primarily Adapter.
    // very similar to strategy as far as class diagram is concerned, but the problem we solve for is different
}
